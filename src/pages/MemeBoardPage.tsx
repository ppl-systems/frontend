import FileExplorer from '../components/meme-board/FileExplorer';
import Canvas from '../components/meme-board/Canvas';
import React, { useState, useRef, useEffect } from 'react';

const MemeBoard: React.FC = () => {
  return (
    <div style={{ display: 'flex', height: '100vh' }}>
    </div>
  );
};

export default MemeBoard;
